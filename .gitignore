target/
build/
.idea/
*.iml
.vscode/
.DS_Store
*.log

# Archived upstream SNAP spec PDFs — local reference only, never committed/redistributed.
# Provenance (version, checksum, ASPI URL) is tracked in docs/snap/snap-1.0.2.json.
docs/snap/sources/

# Tailwind standalone CLI binary (downloaded, not committed)
.tools/

# Local credentials (Cloudflare API token etc.) — never committed
.secrets/
